diff --git a/node_modules/@lerna/publish/index.js b/node_modules/@lerna/publish/index.js
index 16d12a2..59caa76 100644
--- a/node_modules/@lerna/publish/index.js
+++ b/node_modules/@lerna/publish/index.js
@@ -246,8 +246,8 @@ class PublishCommand extends Command {
     });
   }
 
-  verifyWorkingTreeClean() {
-    return describeRef(this.execOpts).then(throwIfUncommitted);
+  verifyWorkingTreeClean(includeMergedTags) {
+    return describeRef(this.execOpts, includeMergedTags).then(throwIfUncommitted);
   }
 
   detectFromGit() {
@@ -256,7 +256,7 @@ class PublishCommand extends Command {
     let chain = Promise.resolve();
 
     // attempting to publish a tagged release with local changes is not allowed
-    chain = chain.then(() => this.verifyWorkingTreeClean());
+    chain = chain.then(() => this.verifyWorkingTreeClean(includeMergedTags));
 
     chain = chain.then(() => getCurrentTags(this.execOpts, matchingPattern));
     chain = chain.then((taggedPackageNames) => {
