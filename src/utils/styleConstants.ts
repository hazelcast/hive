/**
 * Design token values needed in JS/TS logic, not just CSS.
 * Keep in sync with the corresponding --hive-* custom properties in styles/variables.css.
 */
export const styleConstants = {
  colorPrimary: '#2160c0',
  colorSuccess: '#85da66',
  iconStrokeWidth: '1.5px',
  iconStrokeWidthBold: '3px',
  tableColumnMinWidth: 60,
  tableColumnWidth: 170,
  tableColumnMaxWidth: 600,
} as const
