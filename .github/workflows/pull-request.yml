name: check pull request
on:
  pull_request:
    branches: [master]
env:
  CI: true

jobs:
  chromatic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: ./.github/actions/install
      - name: Compile Package
        run: npm run compile
      - name: Build Storybook
        run: npm run build-storybook
      - name: Publish to Chromatic
        uses: chromaui/action@v18
        with:
          token: ${{ github.token }}
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          exitZeroOnChanges: true
          workingDir: ./

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/install
      - run: npm run lint
      - run: npm run compile # should generate artifacts and then use them in the next release?
      - run: npm run test
        env:
          NODE_OPTIONS: --max-old-space-size=8192
# loki does not work with React 19, related to https://github.com/vadimdemedes/ink/issues/688
#  test_visual:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v6
#      - uses: ./.github/actions/install
#      - run: npm run build-storybook
#      - run: export NODE_OPTIONS=--openssl-legacy-provider && npm run test:visual
#
#      - name: Archive screenshots
#        if: ${{ failure() }}
#        uses: actions/upload-artifact@v7
#        with:
#          name: loki-screenshots
#          path: .loki
