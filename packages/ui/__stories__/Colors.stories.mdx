import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks'

import styleConsts from '../styles/constants/export.module.scss'

<Meta title="Visual Style/Colors" />

<ColorPalette>
  {Object.entries(styleConsts)
    .filter(([constName]) => constName.startsWith('color'))
    .map(([colorName, colorValue]) => (
      <ColorItem title={colorName} colors={[colorValue]} />
    ))}
</ColorPalette>
