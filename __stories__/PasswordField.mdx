import { Meta, Title, Canvas, Controls } from '@storybook/blocks'
import * as PasswordFieldStories from './PasswordField.stories'
import { DocsTabs } from '../.storybook/DocsTabs'

<Meta of={PasswordFieldStories} name="Docs" />

<Title />

PasswordField is a masked `TextField` for entering secrets. It renders at a
single compact size and ships with a trailing **eye button** that toggles the
value between hidden and revealed. Use it for passwords, API keys, and any
credential a user types. For non-secret single-line input use `TextField`.

<DocsTabs tabs={[
  {
    id: 'overview',
    label: 'Overview',
    content: (
      <>
        <Canvas of={PasswordFieldStories.Playground} />

        <h2>Basic usage</h2>

        <p>PasswordField is a controlled component. Provide <code>value</code> and an <code>onChange</code> handler that updates state. Pair every field with a visible <code>label</code>. The field renders at a single compact size — there is no <code>size</code> prop.</p>

        <Canvas of={PasswordFieldStories.Basic} />

        <h2>Visibility toggle</h2>

        <p>The trailing eye button switches between masked and revealed text. <code>initiallyVisible</code> sets the starting state. The button has a circular hover and focus ring so the hit target reads clearly as a control, matching the stepper affordance on <code>NumberField</code>.</p>

        <Canvas of={PasswordFieldStories.Visibility} />

        <h2>Leading lock icon</h2>

        <p>Set <code>withIcon</code> to render a leading lock glyph that reinforces the field holds a secret.</p>

        <Canvas of={PasswordFieldStories.WithLockIcon} />

        <h2>Helper text</h2>

        <p>Use <code>helperText</code> for short hints such as complexity rules. The help icon renders inline with the label and reveals the message in a tooltip.</p>

        <Canvas of={PasswordFieldStories.WithHelperText} />

        <h2>Error</h2>

        <p>Set <code>error</code> to surface a validation message inline. The input becomes <code>aria-invalid</code> and links to the message via <code>aria-errormessage</code>.</p>

        <Canvas of={PasswordFieldStories.WithError} />

        <h2>Disabled</h2>

        <p>Disable a field when its value cannot be edited in the current context. The eye button is disabled alongside the input.</p>

        <Canvas of={PasswordFieldStories.Disabled} />

        <h2>Without toggle</h2>

        <p>Set <code>hideToggle</code> to drop the eye button entirely. Reserve this for flows where revealing the value is a security risk.</p>

        <Canvas of={PasswordFieldStories.WithoutToggle} />

        <hr />

        <h2>Visual states</h2>

        <p>QA matrix for engineers verifying CSS changes. Interactive states are forced via classes that mirror the real <code>:hover</code> and <code>:focus</code> rules.</p>

        <Canvas of={PasswordFieldStories.States} />

        <h2>With Formik</h2>

        <p>Use <code>PasswordFieldFormik</code> for forms managed by Formik. It wires <code>value</code>, <code>onChange</code>, and validation errors automatically — pass <code>name</code> and an optional <code>validate</code> function.</p>

        <Canvas of={PasswordFieldStories.WithFormik} />

        <h2>Do &amp; Don&rsquo;t</h2>

        <Canvas of={PasswordFieldStories.DoVsDont} />

        <h2>Props</h2>

        <Controls of={PasswordFieldStories.Playground} />
      </>
    ),
  },
  {
    id: 'guidelines',
    label: 'Guidelines',
    content: (
      <>
        <h2>Anatomy</h2>

        <p>A PasswordField is composed of:</p>

        <ul>
          <li>a <strong>label</strong> — names the field; required for every password input</li>
          <li>an optional <strong>helper</strong> — a tooltip-on-icon that explains complexity rules</li>
          <li>the <strong>input</strong> — the masked single-line control</li>
          <li>an optional <strong>leading lock icon</strong> — reinforces that the value is a secret</li>
          <li>a <strong>reveal toggle</strong> — the trailing eye button that shows or hides the value</li>
          <li>an optional <strong>error message</strong> — surfaces validation inline</li>
        </ul>

        <h2>When to use a PasswordField</h2>

        <ul>
          <li>For passwords, passphrases, API keys, tokens, and other secrets the user types.</li>
          <li>Whenever masking the value on screen reduces shoulder-surfing risk.</li>
        </ul>

        <h3>When <em>not</em> to use a PasswordField</h3>

        <ul>
          <li>For non-secret single-line text → use <code>TextField</code>.</li>
          <li>For numeric secrets where stepping makes sense (rare) → still prefer PasswordField; do not use <code>NumberField</code>.</li>
        </ul>

        <h2>Single size</h2>

        <p>PasswordField renders at one compact size to keep sign-in and credential forms consistent. There is no <code>size</code> prop — if you need a different field height, you are probably building a non-credential input and should use <code>TextField</code>.</p>

        <h2>The reveal toggle</h2>

        <ul>
          <li>Keep the toggle available by default — letting users verify what they typed reduces errors and is accepted security guidance.</li>
          <li>Only set <code>hideToggle</code> when revealing the value is a genuine risk (e.g. shared kiosk screens).</li>
          <li>Do not auto-reveal on focus; let the user opt in.</li>
        </ul>

        <h2>Labels and placeholders</h2>

        <ul>
          <li>Always provide a visible <strong>label</strong>.</li>
          <li>Use the placeholder as a light hint, not as the label.</li>
          <li>Surface real complexity rules via <code>helperText</code>, not the placeholder.</li>
        </ul>
      </>
    ),
  },
  {
    id: 'accessibility',
    label: 'Accessibility',
    content: (
      <>
        <p>PasswordField wraps the v4 <code>TextField</code> with a masked input and an <code>IconButton</code> reveal toggle. Most accessibility comes for free — the notes below cover the password-specific pieces.</p>

        <h2>Accessible name</h2>

        <p>The visible label is the accessible name. Never omit the label. The reveal button has its own accessible name driven by <code>showIconLabel</code> / <code>hideIconLabel</code> (&ldquo;Show password&rdquo; / &ldquo;Hide password&rdquo;) so screen-reader users know what it does and its current state.</p>

        <h2>Keyboard</h2>

        <ul>
          <li><code>Tab</code> moves focus to the input, then to the reveal button.</li>
          <li><code>Space</code> or <code>Enter</code> activates the reveal button.</li>
          <li>The button has a visible focus ring; do not remove it.</li>
        </ul>

        <h2>Autocomplete</h2>

        <p>Pass the appropriate <code>autoComplete</code> value (<code>current-password</code> for sign-in, <code>new-password</code> for registration) so password managers behave correctly.</p>

        <h2>Errors</h2>

        <p>When using <code>error</code>, the message is announced via <code>aria-errormessage</code> and the input gets <code>aria-invalid="true"</code>.</p>

        <h2>Disabled fields</h2>

        <p>Disabled PasswordFields are not focusable and the reveal button is disabled too. Only disable when the value is structurally locked.</p>

        <h2>Color contrast</h2>

        <p>Border, text, and the reveal button meet WCAG AA at all interactive states. Don&rsquo;t override the focus halo with custom CSS.</p>
      </>
    ),
  },
]} />
